!!!
%html
  %head
    %title BlueMap - Android App for iBeacon and Bluetooth LE monitoring
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body{ class: ["#{controller_name}_controller", "#{controller_name}_#{action_name}"] }
    - flash.each do |key, value|
      = content_tag(:div, data: { alert: '' }, class: ['a-flash alert-box', key]) do
        = (value + link_to('×', '#', class: :close)).html_safe
    = render 'layouts/header'
    = yield
    = render 'layouts/footer'

    = javascript_include_tag "application"
